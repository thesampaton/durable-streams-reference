{
  "name": "durable-streams-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end integration tests for durable-streams auth proxy stack",
  "scripts": {
    "test": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "@durable-streams/client": "0.2.1",
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
